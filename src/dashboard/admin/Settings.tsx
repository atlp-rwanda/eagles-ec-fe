import React from "react";

import AdminLayout from "../../components/layouts/AdminLayout";

const Settings = (props) => (
  <AdminLayout>
    <section className="w-full mt-20 text-center">Settings page</section>
  </AdminLayout>
);
export default Settings;
